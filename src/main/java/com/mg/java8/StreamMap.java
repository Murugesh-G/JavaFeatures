package com.mg.java8;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class StreamMap {
    public static void main(String[] args) {
        /*List<Integer> list = Arrays.asList(3, 6, 9, 12, 15);
        list.stream().map(integer -> integer * 3).forEach(System.out::println);*/

        /*List<String> list = Arrays.asList("geeks", "gfg", "g", "e", "e", "k", "s");
        List<String> answer = list.stream().map(String::toUpperCase).collect(Collectors.toList());
        System.out.println(answer);*/

        /*List<String> list = List.of("Geeks", "FOR", "GEEKSQUIZ", "Computer", "Science", "gfg");
        list.stream().map(String::length).forEach(System.out::println);*/


    }
}
